# meta-name: Hit Effect
# meta-description: A standard hit effect, that will add an effect to the any hit it is applied to
# meta-default: true

extends HitEffect

func apply_effect(hit_info: HitInfo) -> void:
	pass
